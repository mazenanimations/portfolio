// Load the first animation (Boxing)
var animation1 = lottie.loadAnimation({
    container: document.getElementById('animationContainer1'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    // path: '../assets/Json/Boxing.json'  // Correct path to the Boxing animation JSON file
    
    path: '../assets/Json/Boxing.json'  // Correct path to the Boxing animation JSON file

  });
  
  // Load the second animation (Cardio)
  var animation2 = lottie.loadAnimation({
    container: document.getElementById('animationContainer2'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: '../assets/Json/Cardio JSON.json'  // Correct path to the Cardio animation JSON file
  });
  
  // Load the third animation (Domino Weight Plates)
  var animation3 = lottie.loadAnimation({
    container: document.getElementById('animationContainer3'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: '../assets/Json/Domino Weight Plates JSON.json'  // Correct path to the Domino Weight Plates animation JSON file
  });
  
  // Load the fourth animation (Hand Exercise)
  var animation4 = lottie.loadAnimation({
    container: document.getElementById('animationContainer4'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: '../assets/Json/hand exercise.json'  // Correct path to the Hand Exercise animation JSON file
  });
  
  // Load the fifth animation (Jumping)
  var animation5 = lottie.loadAnimation({
    container: document.getElementById('animationContainer5'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: '../assets/Json/Jumping JSON.json'  // Correct path to the Jumping animation JSON file
  });
  
  // Load the sixth animation (Running Weights)
  var animation6 = lottie.loadAnimation({
    container: document.getElementById('animationContainer6'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: '../assets/Json/Running Weights JSON.json'  // Correct path to the Running Weights animation JSON file
  });
  